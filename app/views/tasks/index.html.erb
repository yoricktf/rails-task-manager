<h1>My Tasks</h1>
<ul>
<% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task)  %><%= link_to  " -- 🗑", task_path(task), method: :delete, data: {confirm: "sure?"}%></li>
  <% end %>
</ul>









<%= link_to "New task", new_task_path %>
